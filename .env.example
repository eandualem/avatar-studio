# Server-only URLs. Keep provider credentials in assistant-runtime.
RUNTIME_URL=http://127.0.0.1:7112
# Updated isolated runtime with conversation_mode_supported and host_tools output.
# Requires assistant-runtime PR #123 or later; old runtimes fail before allocation.
VOICE_RUNTIME_URL=http://127.0.0.1:7115
BODY_RUNTIME_URL=http://127.0.0.1:7114
# Body: Codex subscription only. Voice: separate OpenAI API access.
# Fast-configured 7116 returned default tier in its trial; keep Body 7114.
# See docs/body-fast-mode.md before changing that route.
# See docs/parallel-body-control.md and docs/parallel-body-verification.md.
